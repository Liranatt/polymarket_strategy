"""CEM backtest / optimization plane.

`optimize_cem` (CEM policy search + expanding walk-forward) and the research
engine under `backtesting.pipeline`. The shared, plane-agnostic entry/exit
kernel and feature math live in the top-level `core` package.
"""
